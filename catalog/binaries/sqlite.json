{
  "id": "binary:sqlite",
  "name": "SQLite",
  "version": "system",
  "description": "Embedded SQL database engine",
  "category": "database",
  "installType": "system",
  "binary": "sqlite3",
  "checkCommand": "sqlite3 --version",

  "platforms": {
    "darwin": {
      "strategy": "system",
      "preinstalled": true,
      "bins": ["sqlite3"],
      "agentGuidance": "SQLite is pre-installed on macOS. No installation needed. Use system binary."
    },
    "linux": {
      "strategy": "package-manager",
      "preinstalled": false,
      "bins": ["sqlite3"],
      "packageManager": "apt",
      "package": "sqlite3",
      "agentGuidance": "SQLite is not pre-installed on most Linux distributions. Recommend: sudo apt install sqlite3"
    },
    "win32": {
      "strategy": "bundled",
      "preinstalled": false,
      "bins": ["sqlite3.exe"],
      "agentGuidance": "SQLite is not pre-installed on Windows. Would need to bundle SQLite binary."
    }
  },

  "installInstructions": {
    "darwin": "Pre-installed on macOS. No action needed.",
    "linux": "sudo apt install sqlite3"
  }
}

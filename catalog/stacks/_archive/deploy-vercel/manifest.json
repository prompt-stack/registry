{
  "id": "deploy-vercel",
  "name": "Deploy to Vercel",
  "version": "1.1.0",
  "description": "Deploy web apps to Vercel. Defaults to production (publicly accessible). Use --preview for protected preview deployments.",
  "runtime": "node",
  "command": [
    "node",
    "deploy.js"
  ],
  "provides": {
    "tools": []
  },
  "requires": {
    "binaries": [],
    "secrets": [
      {
        "description": "Personal access token for deploying to Vercel",
        "label": "Vercel API Token",
        "link": "https://vercel.com/account/tokens",
        "name": "VERCEL_TOKEN"
      }
    ]
  },
  "meta": {
    "author": "Prompt Stack",
    "license": "MIT",
    "category": "deployment",
    "tags": [
      "deploy",
      "vercel",
      "hosting"
    ],
    "icon": "🚀"
  },
  "inputs": [
    {
      "name": "project_path",
      "type": "string",
      "description": "Path to the project to deploy",
      "required": true
    },
    {
      "name": "preview",
      "type": "boolean",
      "description": "Deploy as preview (requires authentication to view). Default: false (deploys to production)",
      "required": false,
      "default": false
    }
  ],
  "outputs": [
    {
      "name": "url",
      "type": "string",
      "description": "Deployed URL (public production URL by default)"
    }
  ]
}
